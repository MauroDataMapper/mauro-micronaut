micronaut:
  security:
    enabled: true
  authentication: session
  endpoints:
    login:
      enabled: true
      path: '/authentication/login'
  logout:
    enabled: true
    path: '/authentication/logout'
    get-allowed: true
  redirect:
    enabled: false