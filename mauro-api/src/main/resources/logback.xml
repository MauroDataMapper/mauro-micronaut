<configuration>

    <appender name="AUDIT" class="ch.qos.logback.core.FileAppender">
        <file>logs/audit.log.jsonl</file>
        <append>true</append>
<!--        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/audit.log.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxHistory>20</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
-->
        <encoder class="org.maurodata.audit.MauroJsonLogEncoder">
        </encoder>
    </appender>

      <appender name="ASYNC-AUDIT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="AUDIT" />
      </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!--<withJansi>true</withJansi>-->
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%cyan(%d{HH:mm:ss.SSS}) %green([%thread]) %highlight(%-5level) %magenta(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="audit" level="trace" additivity="false">
        <appender-ref ref="AUDIT"/>
    </logger>

    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- If this is set to debug, then tests for cache performance will fail! -->
    <logger name="io.micronaut.cache" level="info" />
    <logger name="io.micronaut.data" level="debug" />
    <logger name="io.micronaut.data.query" level="debug" />
    <logger name="io.micronaut" level="info" />
    <logger name="io.micronaut.http.client.netty" level="debug" />
    <logger name="io.micronaut.security" level="debug" />

    <logger name="org.flywaydb" level="info" />

    <logger name="org,maurodata" level="debug" />
    <logger name="org.codehaus.groovy" level="debug" />

</configuration>
